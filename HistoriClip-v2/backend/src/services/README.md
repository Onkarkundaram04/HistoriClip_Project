# Services Layer

This folder is reserved for **business logic services** — reusable functions
that sit between controllers and models.

Use this when controller logic gets complex and needs to be shared
across multiple routes (e.g., email notifications, payment processing,
external API integrations).

Currently empty — controllers handle business logic directly,
which is appropriate for the current project scale.
